To run the tests using command line use the following line of code

if chromedriver is configured in framework.properties file

```
mvn test
```


if chromedriver is configured in framework.properties file

```
mvn test -Dwebdriver.chrome.driver=/Users/e005962/Downloads/chromedriver
```